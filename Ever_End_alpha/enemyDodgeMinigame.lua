

-- USED as a "dummy" state, player state has full control while this is running
local sm = enemy.sm
local ac = enemy.ac

return {
  enter = function(self)
  end,

  update = function(self, dt)
  end,

  exit = function (self)
  end,
}